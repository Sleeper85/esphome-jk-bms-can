logger:
  baud_rate: 0 # Release 'uart esp 0' and prevent boot loops

uart:
  - id: uart_esp_0
    tx_pin: GPIO43
    rx_pin: GPIO44
    baud_rate: 115200

  - id: uart_esp_1
    tx_pin: GPIO17
    rx_pin: GPIO18
    baud_rate: 115200
    
  - id: uart_esp_2
    tx_pin: GPIO15
    rx_pin: GPIO16
    baud_rate: 115200
